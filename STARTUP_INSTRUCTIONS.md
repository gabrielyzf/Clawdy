# Startup Instructions

At the start of every session, read ./MEMORY.md if it exists. Treat it as durable memory and project context. Do not overwrite it unless the user explicitly asks or a durable architecture/project decision has been made.
